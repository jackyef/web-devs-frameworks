import { CodeSurferLayout } from "code-surfer";
export { draculaFull as theme } from "code-surfer/themes";
import { Image, Appear, Notes } from "@mdx-deck/components";

import SocialMediaContact from "./components/SocialMediaContact.jsx";

# Digging Deeper into Hooks :atom_symbol:

<SocialMediaContact />

---

# So JSConf...

<ul>
  <Appear>
    <li>
      <a
        href="https://codepen.io/alexpopovich/full/yoXbZd"
        target="_blank"
      >
        CSS clipping, masking, etc...
      </a>
    </li>
    <li>
      <a
        href="https://codepen.io/mandymichael/full/YYaWop"
        target="_blank"
      >
        Variable Fonts...
      </a>
    </li>
    <li>
      <a
        href="https://aijs.rocks/inspire/evolution-simulator/"
        target="_blank"
      >
        Machine Learning and AI in JS...
      </a>
      (check https://aijs.rocks/)
    </li>
    <li>
      <a href="https://squoosh.app/" target="_blank">
        WebAssembly...
      </a>
    </li>
    <li>JS UI Libraries/Frameworks...</li>
    <li>Knowledge about JS in general...</li>
  </Appear>
</ul>

<Notes>
  Showcase one by one very shortly. 
  The point is to share that these complicated stuffs are now possible in the browser! 
  Web development isn't as simple as in the past anymore.

The AI JS one is about improving Artificial Neural Network over time.

</Notes>

---

# What this talk is going to be about

<Appear>
  <h3>Not those stuffs ❌</h3>
  <h3>React Hooks! ✅</h3>
</Appear>

---

#


---

# To understand more about 
# the tools we use

---

<CodeSurferLayout>

```js title="This is a title"
function hello(name) {
  console.log("hello " + name);
}

hello("world");
```

```js 1:3 subtitle="This is a subtitle"
function hello(name) {
  console.log("hello " + name);
}

hello("world");
```

```js subtitle="New code is highlighted by default"
function helloFactory() {
  return name => {
    console.log("hello " + name);
  };
}

function hello(name) {
  console.log("hello " + name);
}

hello("world");
```

```js subtitle="Adding more code so I can show you how the zoom works"
function helloFactoryBuilder() {
  return () => () => name => {
    console.log("hello " + name);
  };
}

function helloBuilder() {
  return name => {
    console.log("hello " + name);
  };
}

function helloFactory() {
  return name => {
    console.log("hello " + name);
  };
}

function hello(name) {
  console.log("hello " + name);
}

hello("world");
```

```js subtitle="Adding more code so I can show you how the zoom works"
function helloFactoryBuilder() {
  return () => () => name => {
    console.log("hello " + name);
  };
}

function helloBuilder() {
  return name => {
    console.log("hello " + name);
  };
}

function helloFactory() {
  return name => {
    console.log("hello " + name);
  };
}

function hello2(name) {
  console.log("hello " + name);
}

function hello(name) {
  console.log("hello " + name);
}

hello("world");
```

```js 1:23
function helloFactoryBuilder() {
  return () => () => name => {
    console.log("hello " + name);
  };
}

function helloBuilder() {
  return name => {
    console.log("hello " + name);
  };
}

function helloFactory() {
  return name => {
    console.log("hello " + name);
  };
}

function hello(name) {
  console.log("hello " + name);
}

hello("world");
```

</CodeSurferLayout>
