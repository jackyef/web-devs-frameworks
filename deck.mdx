import { CodeSurferLayout } from "code-surfer";
export { draculaFull as theme } from "code-surfer/themes";
import { Image, Appear, Notes } from "@mdx-deck/components";
import { Split } from '@mdx-deck/layouts'

import SocialMediaContact from "./components/SocialMediaContact.jsx";
import SwyxProfile from "./components/SwyxProfile";

# Digging Deeper into Hooks :atom_symbol:

<SocialMediaContact />

---

### I was at JSConf Asia :tada:
<Image src={require("file-loader!./assets/js-conf-2.jpeg")} style={{ backgroundSize: 'contain', height: '72vh' }}/>

---

# So JSConf...

<ul>
  <Appear>
    <li>
      <a
        href="https://codepen.io/alexpopovich/full/yoXbZd"
        target="_blank"
      >
        CSS clipping, masking, etc...
      </a>
    </li>
    <li>
      <a
        href="https://codepen.io/mandymichael/full/YYaWop"
        target="_blank"
      >
        Variable Fonts...
      </a>
    </li>
    <li>
      <a
        href="https://aijs.rocks/inspire/evolution-simulator/"
        target="_blank"
      >
        Machine Learning and AI in JS...
      </a>
      (check https://aijs.rocks/)
    </li>
    <li>
      <a href="https://squoosh.app/" target="_blank">
        WebAssembly...
      </a>
    </li>
    <li>UI Libraries/Frameworks...</li>
    <li>Knowledge about JS in general...</li>
  </Appear>
</ul>

<Notes>
  Showcase one by one very shortly. 
  The point is to share that these complicated stuffs are now possible in the browser! 
  Web development isn't as simple as in the past anymore.

The AI JS one is about improving Artificial Neural Network over time.

</Notes>

---

# What this talk is going to be about

<Appear>
  <h3>Not those stuffs ❌</h3>
  <h3>React Hooks! ✅</h3>
</Appear>

---

# Why am I giving this talk?
<Appear>
  <h3>💰 Because Tokopedia paid for my expenses 💰</h3>
  <h1>🤭</h1>
</Appear>

---

# To understand more about 
# the tools we use

---

<Split>
  <SwyxProfile />
  <div style={{ width: '36vw', padding: '' }}><h2>@swyx</h2></div>
</Split>

---

<div style={{ display: 'flex', flexDirection: 'row', padding: '4vw', justifyContent: 'space-around' }}>
  <Image src={require("file-loader!./assets/hooks-video.png")} style={{ width: '48vw', height: '36vw', backgroundSize: 'contain' }} />
  <Image src={require("file-loader!./assets/hooks-docs.png")} style={{ width: '48vw', height: '36vw', backgroundSize: 'contain' }} />
</div>

---

<CodeSurferLayout>

```js title="This is a class component with state"
class Hello extends React.Component {
  state = {
    count: 0,
  };
  
  render() {
    const { count } = this.state;

    return (
      <div>Current count: {count}</div>
    )
  }
}
```

```js 2:4 subtitle="we have this state..."
class Hello extends React.Component {
  state = {
    count: 0,
  };
  
  render() {
    const { count } = this.state;

    return (
      <div>Current count: {count}</div>
    )
  }
}
```

```js 10 subtitle="and we render it here"
class Hello extends React.Component {
  state = {
    count: 0,
  };
  
  render() {
    const { count } = this.state;

    return (
      <div>Current count: {count}</div>
    )
  }
}
```

```js subtitle="This is a function component"
const Hello = () => {
  return (
    <div>Current count: 0</div>
  )
}
```

```js subtitle="now we add a useState() hook"
const Hello = () => {
  const [count, setCount] = useState(0);

  return (
    <div>Current count: 0</div>
  )
}
```

```js subtitle="use the value..."
const Hello = () => {
  const [count, setCount] = useState(0);

  return (
    <div>Current count: {count}</div>
  )
}
```

```js 1:9 subtitle="And now we have a function component with state"
const Hello = () => {
  const [count, setCount] = useState(0);

  return (
    <div>Current count: {count}</div>
  )
}
```

</CodeSurferLayout>

---

<Image src={require("file-loader!./assets/tweet-markdalgleish-hooks.jpg")} style={{ backgroundSize: 'contain' }}/>

---

### Let's have a refresh on closure! ❄️

---

> (Closure) makes it possible for a function to have "private" variables. 
> 
> -- W3Schools.com

---

### Let's try doing an implementation of hook by ourselves for learning 🤔

---

<CodeSurferLayout>

```js
```

```js
/**
 * Okay, let's start coding! 💻
 */
```

</CodeSurferLayout>